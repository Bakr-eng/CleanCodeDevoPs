name: Context Demo
on: workflow_dispatch

jobs: 
  print-some-stuff:
    runs-on: ubuntu-slim
    steps:
      - name: Step 1
        run: echo "Hello world, from our workflow"


      - name: print som context 
        run: |
          echo "Repository: ${{ github.action }}"
          
        
